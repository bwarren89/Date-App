#if !canImport(SwiftUI)
import Foundation

// Fallback CLI entry point so `swift build` succeeds on platforms
// without SwiftUI (like Linux)
print("SanJoseDatePlanner requires SwiftUI and is best run on macOS or iOS.")
#endif
